export interface TOurTeam {
  id: number;
  image: string;
  color: string;
  name: string;
  role: string;
}

export interface TOurValue {
  id: number;
  image: string;
  title: string;
  description: string;
}
